# NAME: Claude (Pure)
# UPDATED: 2025-12-18
# 策略：必须走美国/英国/新加坡的原生节点 (住宅IP最佳)，严禁频繁变动

# ---- Core Service (核心服务) ----
DOMAIN-SUFFIX,anthropic.com    # 涵盖了 api, console, www 等
DOMAIN-SUFFIX,claude.ai        # 网页版主域
DOMAIN-SUFFIX,claudeusercontent.com # 附件、Artifacts 预览
DOMAIN-SUFFIX,claude.user.content # [新增] 部分新版资源域名

# ---- Infrastructure (部分必要的基建) ----
# 仅保留 Claude 强依赖且较少误伤的
DOMAIN-SUFFIX,usefathom.com    # Claude 网页版常用的无隐私分析（可选）

# ---- 必须删除的垃圾 ----
# cloudflare.net (绝对不能加，这是自杀)
# auth0.com / okta.com (通用鉴权，别劫持)
# intercom.io (通用客服，别劫持)
# segment.io / datadoghq.com (通用埋点，浪费流量)
# chatgpt.livekit.cloud (这是OpenAI的，走错片场了)
# arkoselabs.com (虽然 Claude 用它做验证，但建议走全局规则，不要强制绑定 Claude 节点，否则会误伤 Twitter 等其他网站)
