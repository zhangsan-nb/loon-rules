# NAME: Curve (Europe/DE)
# UPDATED: 2025-12-18
# 策略：必须走 欧洲(EU)/德国(DE) 原生 IP，且保持长连接稳定

# ---- Core Service (核心业务) ----
DOMAIN-SUFFIX,curve.com
DOMAIN-SUFFIX,curve.app            # 短链与App关联
DOMAIN-SUFFIX,imaginecurve.com     # 公司旧实体名，API 仍大量使用
DOMAIN-SUFFIX,curve.systems        # [新增] 后端系统常涉及的域名

# ---- API Specifics (明确的接口) ----
# 这些子域名其实被上面的主域覆盖了，但为了防止DNS解析策略遗漏，可显式保留
# DOMAIN-SUFFIX,api.imaginecurve.com 

# ---- Support (客服集成) ----
# Kustomer 是第三方，但 Curve 用它做核心客服，且域名带 curve 前缀，可留
DOMAIN-SUFFIX,curve.api.kustomerapp.com

# ---- 必须删除的垃圾 ----
# firebaseremoteconfigrealtime.googleapis.com (剧毒，删)
# branch.io (通用服务，删)
# braze.eu (通用服务，删)
# siftscience.com (通用反欺诈，别碰)
# emb-api.com (非 Curve 核心，建议删)
